<server description="new server">

    <!-- Enable features -->
    <featureManager>
        <feature>javaee-8.0</feature>        
        <feature>localConnector-1.0</feature>
    </featureManager>

    <!-- To access this server from a remote client add a host attribute to the following element, e.g. host="*" -->
    <httpEndpoint httpPort="8080" httpsPort="9444" id="defaultHttpEndpoint"/>

    <!-- Automatically expand WAR files and EAR files -->
    <applicationManager autoExpand="true"/>

    <applicationMonitor updateTrigger="mbean"/>
    
    <dataSource id="DefaultDataSource" jndiName="jdbc/torganizerDS">
    	<jdbcDriver libraryRef="MySQLLib"/>
        <properties databaseName="torganizer" serverName="localhost" portNumber="3306"/>
    </dataSource>
    <library id="MySQLLib"><file name="c:/dev/server/mysql-connector-java-5.1.45-bin.jar"/></library>

    <keyStore id="defaultKeyStore" password="{xor}AD47MjYxAA=="/>

    <basicRegistry id="basic" realm="BasicRealm">
        <user name="admin" password="{xor}PjsyNjE="/>
    </basicRegistry>

    <webApplication id="rest" location="rest.war" name="rest"/>

    <webApplication id="webclient" location="webclient.war" name="webclient"/>
</server>